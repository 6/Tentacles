%table.table.table-hover.browse-list
  %thead
    %tr
      %th= link_to_order("Status", "/", "status")
      %th= link_to_order("Name", "/", "title")
      %th= link_to_order("Created", "/", "created_at")
      %th= link_to_order("Updated", "/", "updated_at")
  %tbody
    - @scrapers.each do |scraper|
      %tr
        %td TODO
        %td
          %a.scraper-link{:href => scraper.path}= scraper.title
        %td= scraper.created_at
        %td= scraper.updated_at


.pagination
  - if @page > 1
    %a{:href => "/?page=#{@page - 1}"} Prev
  = "Page #{@page} of #{@page_count}"
  - if @page < @page_count
    %a{:href => "/?page=#{@page + 1}"} Next
